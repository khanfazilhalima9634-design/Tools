## Packages
framer-motion | Smooth layout transitions and entry animations
recharts | For visualizing the ATS score with a radial chart
lucide-react | Icons for the UI (already in base, but confirming usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
